{
  "name": "naji-liquid-crystal-api",
  "version": "1.0.0",
  "private": true,
  "description": "NAJI liquid-crystal glassmorphism sound interface with server-side API key generation.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "prepare:web": "node scripts/build-public.js",
    "android:sync": "npm run prepare:web && npx cap sync android"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
